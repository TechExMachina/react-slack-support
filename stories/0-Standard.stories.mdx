import { action } from "@storybook/addon-actions";
import ReactSlackSupport from "../src";

import {
  getSlackMessages,
  postSlackMessage,
  postSlackFile
} from "./fakeServer";

import {
  FaceComponentForComponentsProps,
} from '../src/ComponentsForStories'

import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";

<Meta title="ReactSlackSupport/Standard" component={ReactSlackSupport} />

# React Slack Support

This is the default component to start a live support

<Preview>
  <Story name="Standard">
    <div style={{height: '75vh'}}>
      <ReactSlackSupport
        botName={`Tester`}
        userImage={
          "http://www.iconshock.com/img_vista/FLAT/mail/jpg/robot_icon.jpg"
        }
        defaultMessage={"Hi ! How can i help you ?"}
        getMessage={getSlackMessages}
        postMessage={postSlackMessage}
        postFile={postSlackFile}
      />
    </div>
  </Story>
</Preview>

## Props

<Props of={ReactSlackSupport} />

## Components Object <a id="ComponentsProps" />

<Props of={FaceComponentForComponentsProps} />
