import { action } from "@storybook/addon-actions";
import ReactSlackSupport from "../src";

import Button from '@material-ui/core/Button'

import {
  getSlackMessages,
  postSlackMessage,
  postSlackFile
} from "./fakeServer";

import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";

<Meta title="ReactSlackSupport/OverrideButton" component={ReactSlackSupport} />

# React Slack Support

You can override some component (like call button) to personalize UI

<Preview>
  <Story name="Standard">
    <div style={{height: '75vh'}}>
        <ReactSlackSupport
          botName={`Tester`}
          userImage={
            "http://www.iconshock.com/img_vista/FLAT/mail/jpg/robot_icon.jpg"
          }
          defaultMessage={
            "Hello! Thank you for answering a few questions so that we can help you in the best way possible"
          }
          getMessage={getSlackMessages}
          postMessage={postSlackMessage}
          postFile={postSlackFile}
          components={{
            callButton: <Button variant='contained' color='secondary'>Need help ?</Button>
          }}
        />
    </div>
  </Story>
</Preview>
