import { action } from "@storybook/addon-actions";
import ReactSlackSupport from "../src";

import {
  getSlackMessages,
  postSlackMessage,
  postSlackFile
} from "./fakeServer";

import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";

<Meta title="ReactSlackSupport/WithQuestion" component={ReactSlackSupport} />

# React Slack Support

You can ask to your visitor some questions before start the discussion. That can help you to answer faster or to redirect to the good hotliner

<Preview>
  <Story name="Standard">
    <div style={{height: '75vh'}}>
        <ReactSlackSupport
          botName={`Tester`}
          userImage={
            "http://www.iconshock.com/img_vista/FLAT/mail/jpg/robot_icon.jpg"
          }
          defaultMessage={
            "Hello! Thank you for answering a few questions so that we can help you in the best way possible"
          }
          defaultAsk={[
            {
              question: "Are you more salt or pepper?",
              answer: ["Salt", "Pepper"]
            },
            {
              question: "Is your question about a bug or a feedback?",
              answer: ["Bug", "Feedback"]
            }
          ]}
          getMessage={getSlackMessages}
          postMessage={postSlackMessage}
          postFile={postSlackFile}
        />
    </div>
  </Story>
</Preview>
